{
  "name": "zustand-state-tree",
  "homepage": "https://js.work",
  "version": "1.0.0",
  "description": "Zustand state tree.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "scripts": {
    "dev": "cd packages/example && yarn run dev",
    "predocs": "rm -rf docs",
    "docs": "cd packages/example && yarn build",
    "postdocs": "cp -R packages/example/dist docs",
    "build": "cd packages/lib && yarn build",
    "publish": "cd packages/lib && yarn run release"
  }
}
